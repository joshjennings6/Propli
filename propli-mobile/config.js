// API configuration for the ProPli mobile app.
// Set API_BASE to point to your backend server. When testing locally
// with Expo, replace 'localhost' with your machine's LAN IP.
export const API_BASE = 'http://localhost:5000/api';
